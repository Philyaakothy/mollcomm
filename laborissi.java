sb.append("<a href='#' class='on'>")  // Append the opening <a> tag with href="#" and class="on"
  .append(i)                          // Append the value of variable i
  .append("</a>")                     // Append the closing </a> tag
  .append("\n");                      // Append a newline character for formatting purposes
